---
// Matrix Config - Script Hosting Page
const version = '4.9.32';
const scripts = [
  { name: 'main.build.js', description: 'Main application script' },
  { name: 'productsPage.build.js', description: 'Products page functionality' }
];
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Matrix Config Scripts</title>
  <meta name="robots" content="noindex, nofollow">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      line-height: 1.6;
      background: #f8f9fa;
    }
    .container {
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    h1 { color: #333; }
    .script-link {
      display: block;
      margin: 10px 0;
      padding: 15px;
      background: #f5f5f5;
      border-radius: 8px;
      text-decoration: none;
      color: #333;
      border: 1px solid #e0e0e0;
      transition: all 0.2s ease;
    }
    .script-link:hover {
      background: #e5e5e5;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    pre {
      background: #1e1e1e;
      color: #d4d4d4;
      padding: 20px;
      border-radius: 8px;
      overflow-x: auto;
    }
    code { font-family: 'Monaco', 'Menlo', monospace; }
    .badge {
      display: inline-block;
      background: #4caf50;
      color: white;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      margin-left: 10px;
    }
    .info {
      background: #e3f2fd;
      border-left: 4px solid #2196f3;
      padding: 15px;
      margin: 20px 0;
      border-radius: 0 8px 8px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Matrix Config Scripts <span class="badge">v{version}</span></h1>
    <p>JavaScript files for the Matrix Config application, deployed on Webflow Cloud.</p>

    <div class="info">
      <strong>Webflow Cloud Deployment</strong><br>
      These scripts are served from Webflow's global edge network for optimal performance.
    </div>

    <h2>Available Scripts</h2>
    {scripts.map(script => (
      <a href={`/${script.name}`} class="script-link">
        <strong>{script.name}</strong><br>
        <small>{script.description}</small>
      </a>
    ))}

    <h2>Webflow Integration</h2>
    <p>Add these script tags to your Webflow page's Custom Code section:</p>
    <pre><code>{scripts.map(s => `<script src="https://YOUR_WEBFLOW_CLOUD_URL/${s.name}"></script>`).join('\n')}</code></pre>

    <p style="color: #666; font-size: 14px; margin-top: 30px;">
      Deployed via Webflow Cloud | Auto-updated from GitHub
    </p>
  </div>
</body>
</html>
